@model TransafeRx.Models.SurveyQuestionLogicActionViewModel

@(
    Html.Kendo().DropDownListFor(x => x)
        .DataValueField("ActionId")
        .DataTextField("Name")
        .DataSource(source =>
        {
            source.Read(x => x.Action("SurveyQuestionLogicActions", "Item"));
        })
        .Events(events => events.Change("questionLogicAction_change"))
)